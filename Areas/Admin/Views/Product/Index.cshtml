@model List<Product>
@{
    ViewBag.Title = "Меню кафе";
}

<h2>Список товаров</h2>

<p>
    @Html.ActionLink("Создание товара", "Create", "Product", new { area = "Admin" }, new { @class = "btn btn-primary " })
</p>

<table class="table-bordered  table-hover ">
    <tr>
        <td>Наименование</td>
        <td>Описание</td>
        <td>Количество</td>
        <td>Цена</td>
        <td>Категория</td>
        <td>Действие</td>
    </tr>
    @foreach (var item in Model)
    {
<tr>
    <td>@item.Caption</td>
    <td>@item.Description</td>
    <td>@item.Quantity</td>
    <td>@item.Price руб.</td>
    <td>@item.ProductCategory.Caption</td>
    <td>
        @Html.ActionLink("Редактировать", "Edit", "Product", new { area = "Admin", id = item.Id }, new { @class = "btn btn-success " })
        @Html.ActionLink("Удалить", "Remove", "Product", new { area = "Admin", id = item.Id }, new { @class = "btn btn-danger " })
    </td>
</tr>

    }
</table>

